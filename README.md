# DAResourceSchedulingExp

To run:
run `build.dat`
then `build\sim.exe`

Settings:
edit `config.txt`, no need to re-build

To plot:
Install python with `pandas` and `matplotlib`
then `python plot_prop.py` or `python plot_index.py`