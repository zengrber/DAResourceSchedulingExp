# DAResourceSchedulingExp

To run:
run `build.bat`
then `build\sim.exe ${CONFIG_FILENAME}`

Settings:
edit `config.txt`, no need to re-build

To plot:
Install python with `pandas` and `matplotlib`
then `python plot_${TYPENAME}.py`
Recommend `python plot_PA.py`
